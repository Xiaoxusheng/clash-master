# Clash Master Collector 配置（可选）
# 默认端口即可满足大部分需求

# API 服务端口（默认 3001）
# API_PORT=3001

# WebSocket 推送端口（默认 3002）
# COLLECTOR_WS_PORT=3002

# SQLite 数据库路径（默认当前目录）
# DB_PATH=./stats.db

# ⚠️ 注意：OpenClash 后端配置通过 Web 界面动态添加
# 无需在此文件配置 CLASH_URL 或 CLASH_TOKEN
